import React from 'react';
import Layout from '../Layout/Layout';

const ProductList = () => {
    return (
        <Layout>
            Product List
        </Layout>
    )
}

export default ProductList;